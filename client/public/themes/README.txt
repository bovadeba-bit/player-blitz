プレイヤー custom default backgrounds

Drop your four preferred default background images in this folder using exactly:
  theme-1.jpg
  theme-2.jpg
  theme-3.jpg
  theme-4.jpg

The Activity already has gradient fallbacks, so it works even before you add them.
These four defaults are local appearance choices; the + button in the Activity lets each user load a private local custom image.
